// todo Менше з чисел
/*
 * Напиши функцію min(a,b), яка повертає найменше з чисел a та b.
 */

function min(a, b) {
  // if (a < b) {
  //   return a;
  // }

  // return b;

  // return a < b ? a : b;

  return Math.min(a, b);
}

console.log(min(2, 6));
console.log(min(20, 1));
